<h1><%= @attraction.name %> Show Page</h1>

<!--You must be very specific about the target object, controller, action, and method to get this to work-->
<%= form_for(:ride, controller: 'ride', action: 'create', method: 'post') do |f| %>
  <%= f.submit "Go on this ride" %>
<% end %>

<!--After all the tests pass, this still breaks when not logged in-->
  <%= link_to "Edit Attraction", edit_attraction_path if User.find(session[:user_id]).admin %>

